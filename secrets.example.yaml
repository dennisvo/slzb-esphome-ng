# Template for secrets.yaml — copy to secrets.yaml and fill in real values.
# secrets.yaml is gitignored (.gitignore line 5).
#
# Required by the slzb-esphome-ng MR4U firmware (packages/core/core.yaml).

# ESPHome Native API encryption key.
# Generate one at https://esphome.io/components/api.html (or run
# `python -c "import secrets, base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"`).
# Home Assistant stores the same value when adopting the device.
api_encryption_key: "REPLACE_WITH_32_BYTE_BASE64_KEY"

# OTA password. Any strong string. Required for `esphome upload` after
# initial flash.
ota_password: "REPLACE_WITH_STRONG_PASSWORD"

# Optional: Wi-Fi credentials (only if using packages/wifi/wifi.yaml
# instead of Ethernet).
# wifi_ssid: "your-ssid"
# wifi_password: "your-wifi-password"
